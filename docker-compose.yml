version: "3.2"
services:
  react-app:
    container_name: germaniii-website
    build:
      context: ./root
      dockerfile: Dockerfile
    ports:
      - 8000:3000
    volumes:
      - ./root:/app
      - /app/node_modules
